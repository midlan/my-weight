{
  "name": "my-weight-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright tests for the my-weight app. Loads the source HTML in Chromium and calls top-level functions via page.evaluate — no full UI smoke (the deploy workflow already covers that).",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "install-browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
